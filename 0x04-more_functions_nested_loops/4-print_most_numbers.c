#include "main.h"

/**
 * print_most_numbers- prints numbers from 0 to 8
 * (neither 2 0r 4)
 * Return: no return
 */

void print_most_numbers(void)
{
	int ch;

	for (ch = 48; c < 58; c++)
	{
		if (ch != 50 && c != 52)
			_putchar(ch);
	}
	_putchar("\n");
}
